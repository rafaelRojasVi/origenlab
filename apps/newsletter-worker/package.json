{
  "name": "origenlab-newsletter-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "validate": "npm run typecheck && npm test",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260722.1",
    "@types/node": "^24.13.6",
    "typescript": "^5.9.3",
    "vitest": "^4.1.9",
    "wrangler": "^4.121.0"
  }
}
