# Dashboard v1 → apps/api (read-only)
#
# Local dev (npm run dev):
#   Leave VITE_ORIGENLAB_API_BASE_URL unset so the Vite proxy forwards
#   /health, /operator, /cases, /opportunities to http://127.0.0.1:8001.
#   Restart `npm run dev` after any .env change.
#
# VITE_ORIGENLAB_API_BASE_URL=
#
# Do NOT point local dev at :8000 (legacy email-pipeline API) — requests will fail.
#
# Production / preview build (npm run build):
#   Required — set to same-origin read-only proxy base (recommended):
#
# VITE_ORIGENLAB_API_BASE_URL=https://dashboard.origenlab.cl/api
#
# Deploy apps/dashboard-proxy Worker first (see apps/dashboard-proxy/README.md).
# Direct API host (cross-origin, no browser token) is not supported in production:
# VITE_ORIGENLAB_API_BASE_URL=https://api.example.com
#
# Auth tokens must NOT be exposed through VITE_* variables — they are embedded in
# the static JS bundle at build time. ORIGENLAB_API_AUTH_TOKEN belongs in server
# secrets (API origin or a read-only BFF/proxy), not in dashboard .env for the browser.
# See docs/PRODUCTION_API_AUTH.md.
