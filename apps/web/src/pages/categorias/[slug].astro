---
import Layout from '../../layouts/Layout.astro';
import PageIntro from '../../components/ui/PageIntro.astro';
import SectionIndex from '../../components/ui/SectionIndex.astro';
import ModelList from '../../components/ui/ModelList.astro';
import ModelRow from '../../components/ui/ModelRow.astro';
import ClosingQuoteBand from '../../components/ui/ClosingQuoteBand.astro';
import Notice from '../../components/ui/Notice.astro';
import Icon from '../../components/ui/Icon.astro';
import { categories } from '../../data/categories';
import { company } from '../../data/company';
import { documentsForCategory } from '../../data/documents';
import { getCategoryRelatedProducts } from '../../lib/catalog';

export function getStaticPaths() {
  return categories.map((category) => ({ params: { slug: category.slug } }));
}

const { slug } = Astro.params;
const category = categories.find((item) => item.slug === slug);
if (!category) return Astro.redirect('/aplicaciones');

const related = getCategoryRelatedProducts(category.slug);
const documents = documentsForCategory(category.slug);
const isClinical = category.slug === 'laboratorio-clinico';
const shortName = category.shortName;

const metaDescription =
  `${category.description ?? ''} Cotización y asesoría técnica a través de OrigenLab, con atención en todo Chile.`.slice(
    0,
    158,
  );
---

<Layout title={shortName} description={metaDescription}>
  <div class="shell">
    <PageIntro
      title={category.name}
      lead={category.description}
      crumbs={[{ label: 'Aplicaciones', href: '/aplicaciones/' }, { label: shortName }]}
    />
  </div>

  <section class="shell section rule-top" aria-labelledby="antes-title">
    <SectionIndex index={1} label="Antes de cotizar" />
    <div class="guide">
      <div>
        <h2 id="antes-title" class="t-h2">Qué conviene traer a la conversación</h2>
        <p class="t-body guide__text">{category.buyerGuide}</p>
      </div>
      <ol class="checklist">
        {category.buyerBullets.map((bullet) => <li>{bullet}</li>)}
      </ol>
    </div>
  </section>

  <section class="shell section rule-top" aria-labelledby="equipos-title">
    <SectionIndex index={2} label="Equipos relacionados" />
    {
      related.length > 0 ? (
        <>
          <h2 id="equipos-title" class="t-h2">Centrífugas Ortoalresa</h2>
          <p class="t-lead mt-5">
            Preparación de muestras y separación, según configuración de rotor y requerimientos del
            laboratorio.
          </p>
          <ModelList class="mt-10">
            {related.map((product) => <ModelRow product={product} size="compact" />)}
          </ModelList>
          <a href="/productos/centrifugas/" class="link-arrow mt-8">
            Ver la familia y la comparativa
            <Icon name="arrow-right" size={18} class="arrow" />
          </a>
        </>
      ) : (
        <>
          <h2 id="equipos-title" class="t-h2">Aún sin familia publicada</h2>
          <p class="t-lead mt-5">
            No hay una familia de equipos publicada para esta línea todavía. Cotizamos igualmente:
            indique el ensayo, el volumen de trabajo y la norma asociada si aplica, y le
            respondemos con alternativas concretas.
          </p>
          {isClinical && (
            <p class="t-body clinical">
              Algunos modelos de centrífugas pueden evaluarse para preparación de muestras en
              laboratorio clínico según configuración y requerimientos. Esto no implica
              certificación clínica ni idoneidad diagnóstica sin confirmación del responsable
              técnico y del fabricante.
            </p>
          )}
          <a href="/productos/" class="link-arrow mt-8">
            Ver el catálogo publicado
            <Icon name="arrow-right" size={18} class="arrow" />
          </a>
        </>
      )
    }
  </section>

  <section class="shell section rule-top" aria-labelledby="documentacion-title">
    <SectionIndex index={3} label="Documentación" />
    <h2 id="documentacion-title" class="t-h2">Catálogos y fichas técnicas</h2>
    {
      documents.length > 0 ? (
        <ul class="doc-list">
          {documents.map((document) => (
            <li>
              <a href={document.fileUrl} class="link-arrow">
                <Icon name="file" size={18} />
                {document.title}
              </a>
            </li>
          ))}
        </ul>
      ) : (
        <p class="t-lead mt-5">{company.catalogNote}</p>
      )
    }
    <Notice class="mt-10">
      Cotización y disponibilidad sujetas a confirmación comercial. La configuración, los
      accesorios y las condiciones se confirman en la cotización.
    </Notice>
  </section>

  <ClosingQuoteBand
    index={4}
    title={`Cotizar equipamiento para ${shortName.toLowerCase()}`}
    body="Indique el ensayo o la función que necesita cubrir, el volumen de trabajo y su institución. Con eso acotamos alternativas y preparamos la cotización."
  />
</Layout>

<style>
  .guide {
    display: grid;
    gap: 2.5rem;
  }
  .guide__text {
    margin-top: 1.25rem;
  }
  .checklist {
    list-style: none;
    margin: 0;
    padding: 0;
    counter-reset: item;
    border-top: 1px solid var(--color-ink-950);
  }
  .checklist li {
    counter-increment: item;
    display: grid;
    grid-template-columns: 2rem minmax(0, 1fr);
    gap: 1rem;
    padding-block: 1rem;
    font-size: 0.9375rem;
    line-height: 1.55;
    color: var(--color-ink-800);
    border-bottom: 1px solid var(--color-hairline);
  }
  .checklist li::before {
    content: counter(item, decimal-leading-zero);
    font-family: var(--font-mono);
    font-size: 0.8125rem;
    color: var(--color-teal-800);
  }
  .clinical {
    margin-top: 1.75rem;
    color: var(--color-ink-600);
  }
  .doc-list {
    list-style: none;
    margin: 2rem 0 0;
    padding: 0;
    display: grid;
    gap: 0.75rem;
  }
  @media (min-width: 768px) {
    .guide {
      grid-template-columns: minmax(0, 6fr) minmax(0, 5fr);
      gap: 4rem;
      align-items: start;
    }
  }
</style>
